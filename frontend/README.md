# Frontend

Frontend for BlinkResearch (React + Vite).

For setup, run commands, and architecture notes, use the root docs:

- `../README.md`

Quick local commands:

```bash
npm install
npm run dev
npm run build
```
